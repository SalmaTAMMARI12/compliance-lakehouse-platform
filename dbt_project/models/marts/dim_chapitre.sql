-- DEPRECATED : ce modèle est remplacé par deux modèles distincts :
--   • dim_chapitre_dnssi  → 14 chapitres fixes du référentiel DNSSI v2
--   • fact_chapitre_audit → occurrences de chapitres par audit
-- Ce fichier est conservé temporairement pour compatibilité.
-- Il sera supprimé dans une prochaine version.

{{ config(enabled=false) }}

select 1 as placeholder